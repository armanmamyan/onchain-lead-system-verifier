DATABASE_URL="prisma+postgres://accelerate.prisma...."

# ═══════════════════════════════════════════════════════════════════
# JWT SIGNING KEYS (RS256 - RSA Algorithm)
# Generate RSA key pair:
# 1. Generate private key: openssl genpkey -algorithm RSA -out private.key -pkeyopt rsa_keygen_bits:2048
# 2. Extract public key: openssl rsa -pubout -in private.key -out public.key
# 3. Convert to single-line format (remove headers/newlines):
#    - cat private.key | tr -d '\n' | sed 's/-----BEGIN PRIVATE KEY-----//g' | sed 's/-----END PRIVATE KEY-----//g'
#    - cat public.key | tr -d '\n' | sed 's/-----BEGIN PUBLIC KEY-----//g' | sed 's/-----END PUBLIC KEY-----//g'
# ═══════════════════════════════════════════════════════════════════
PARTNER_PRIVATE_KEY=""
PARTNER_PUBLIC_KEY=""
SIGNING_ALGORITHM="RS256" # RSA algorithm for JWT signing

NEXT_PUBLIC_PARTNER_ID=
NEXT_PUBLIC_ISSUER_DID=
NEXT_PUBLIC_VERIFIER_ID=
NEXT_PUBLIC_SITE_NAME="Oyunfor Gaming Platform"
NEXT_PUBLIC_SITE_DESCRIPTION="Access exclusive Web3 games with verified on-chain credentials"
NEXT_PUBLIC_BUILD_ENV=sandbox
NEXT_PUBLIC_ISSUER_URL=http://localhost:3000
ADMIN_PASS=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
NEXT_PUBLIC_WALLET_KIT_ID=
NEXT_PUBLIC_ORIGIN_URL=
NEXT_PUBLIC_ALCHEMY_ID=
NEXT_PUBLIC_THEME=light
# (optional) Defaults to / if not set
NEXT_PUBLIC_RETURN_URL=
# (optional) Defaults to NEXT_PUBLIC_SITE_NAME if not set
NEXT_PUBLIC_RETURN_SITE_NAME=